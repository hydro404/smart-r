<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>SMART-R</title>

    <!-- Bootstrap -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet"
    />

    <!-- Leaflet -->
    <link
      rel="stylesheet"
      href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
    />
    <link rel="stylesheet" href="css/style.css" />
  </head>
  <body>
    <div id="map"></div>

    <div id="side">
      <div class="panel-header text-center">
        <h1 class="fw-bold">SMART-R</h1>
        <p style="color: #1b5e20;">Spatial Modeling and Analysis for Risk-based Transport Routing</p>
      </div>

      <div class="mb-2">
        <label class="form-label small mb-1">Zone</label>
        <select id="zoneSelect" class="form-select"></select>
      </div>

      <div class="mb-2">
        <label class="form-label small mb-1">Trip</label>
        <select id="tripSelect" class="form-select"></select>
      </div>

      <div class="d-grid gap-2">
        <button id="analyzeBtn" class="btn btn-primary" disabled>
          Analyze Routes
        </button>
      </div>
      <!-- <div class="d-grid gap-2 mt-2">
        <button id="drawSelectedBtn" class="btn btn-success" disabled>
          Draw Selected Trip
        </button>
      </div> -->

      <hr />

      <div id="segmentsContainer">
        <p class="small-muted">
          After analyzing, segments and route alternatives will appear here.
          Click a route to draw it on the map.
        </p>
      </div>
    </div>

    <!-- Libraries -->
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/PapaParse/5.4.1/papaparse.min.js"></script>
    <script src="https://unpkg.com/leaflet.heat/dist/leaflet-heat.js"></script>
    <script src="js/app.js"></script>
  </body>
</html>
